using System.ComponentModel;
using System.Diagnostics.Metrics;
using static System.Runtime.InteropServices.JavaScript.JSType;

namespace ConsoleApp1
{
    using System;
    internal class Program
    {
        static void Main(string[] args)
        {

            #region Q1
               /*
           Write a program that allows the user to enter a number then print it.
            Console.WriteLine("pleas insert number!");
            
            int number = int.Parse(Console.ReadLine());
            Console.WriteLine($"Your number is : {number}");
*/
            #endregion

            #region Q2
            /* 
            Write C# program that converts a string to an integer, but the string contains non-numeric characters. And mention what will happen 
             string str = "123dftg";

              int output;
              if (int.TryParse(str, out output))
              {
                  Console.WriteLine("your number is: " + output);
              }
              else
              {
                  Console.WriteLine("*error* there is charater in your input cannout convert it to a intger");
              }*/
            #endregion



        }


    }
}